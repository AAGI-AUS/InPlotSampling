# This function computes the coefficient of variance estimator

This function computes the coefficient of variance estimator

## Usage

``` r
calculate_coefficients(H, n)
```

## Arguments

- H:

  Set size for each ranking group.

- n:

  Sample size.
